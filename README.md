# create folder
mkdir notebooks
mkdir sql
mkdir powerbi

# add files inside (example)
touch notebooks/data_preparation.ipynb
touch sql/kpi_views.sql

# add to git
git add .

# commit
git commit -m "Add project folders"

# push to github
git push
